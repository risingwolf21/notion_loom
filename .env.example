# Optional: bake in your Cloudflare Worker URL at build time.
# When set, users only need to enter their Notion token (no worker URL step).
# Copy to .env.local for local dev, or set as a GitHub Actions secret.
VITE_WORKER_URL=https://your-worker.workers.dev
